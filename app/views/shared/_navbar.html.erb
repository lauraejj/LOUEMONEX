<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">LoueMonEx.com</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
          <%= link_to "Boyfriends' Home", root_path, class: "nav-link" %>
        </li>
    </ul>

    <ul class="navbar-nav mr-auto">
      <%= form_tag search_path, method: :get do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Find a boyfriend"
        %>
        <%= submit_tag "Search", class: "btn btn-primary" %>
      <% end %>
    </ul>

  <div class="dropdown">
      <% if signed_in? %>
         <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= cl_image_tag current_user.avatar%> Hi, <%= current_user.nickname %> <span class="caret"></span></a>
      <% else %>
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Account Info <span class="caret"></span></a>
      <% end %>
     <% if signed_in? %>
        <ul class="dropdown-menu">
          <a href="/boyfriends/new" class="dropdown-item"><i class="fa fa-user-o"></i> Create a boyfriend</a>
          <a href="reservations/dashboard" class="dropdown-item"><i class="fa fa-sliders"></i> Dashboard</a>
          <div class="divider dropdown-divider"></div>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </ul>
        <ul class="dropdown-menu">
   </ul>
      <% else %>
        <a href="/users/sign_in" class="dropdown-item"><i class="fa fa-user-o"></i> Log In</a>
        <a href="/users/sign_up" class="dropdown-item"><i class="fa fa-signup-o"></i> Sign Up</a>
      <% end %>
    </ul>
  </div>
</nav>

